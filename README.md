# 胜凯装饰动态表单 Android 安装包

本仓库只用于发布胜凯装饰动态表单系统的 Android 正式安装包，不包含应用源码。

请从 [Releases](../../releases) 下载 `dynamic-form.apk`，也可以在 App 的“设置 → 应用更新”中检查新版本。

发布规则：

- Release 标签与 App 版本一致，例如 `v1.2.0`。
- 安装包名称固定为 `dynamic-form.apk`。
- 已发布版本不会覆盖；修复会使用更高版本号。
- 所有可覆盖更新的 APK 均使用同一套正式签名。